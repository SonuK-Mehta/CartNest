# Shopping Cart + Free Gift 🎁 React App

This React app allows users to add products to their cart and unlock a free gift once they hit a spending threshold.

## 🛠️ Installation

1. Clone the repository
2. Run `npm install` to install dependencies
3. Run the app:

```bash
npm run dev  # if using Vite

```
